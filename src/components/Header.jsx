import "../styles/header.css";

const Header = () => {
  return (
    <div className="header">
      <h1 className="title">Ads Dashboard</h1>
      <button className="btn btn-create">Create Ad</button>
    </div>
  );
};

export default Header;
